<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.alibaba.xinan.sirs.mapper.ProductMapper">


    <select id="listAll" parameterType="com.alibaba.xinan.sirs.entity.query.ProductQuery"
            resultType="com.alibaba.xinan.sirs.entity.Product">
        SELECT
        <include refid="all_column"/>
        FROM product
        <where>
            <if test="query.name != null and query.name != '' ">AND name LIKE CONCAT('%', #{query.name}, '%')</if>
            <if test="query.minPrice != null">AND price &gt;= #{query.minPrice}</if>
            <if test="query.maxPrice != null">AND price &lt;= #{query.maxPrice}</if>
            <if test="query.stock != null">AND stock >= #{query.stock}</if>
            <if test="query.description != null and query.description != '' ">
                AND description LIKE CONCAT('%', #{query.description}, '%')
            </if>
            <if test="query.categoryId != null">AND category_id = #{query.categoryId}</if>
        </where>
    </select>

    <sql id="all_column">
        id, name, price, stock, description, status, category_id, create_time, update_time, is_del
    </sql>
</mapper>